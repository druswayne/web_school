# § 19. Функция

**Страницы учебника:** 210–230
**Глава:** 3. Линейные уравнения. Линейные неравенства

## Определения
### Определение. Зависимость между двумя переменными, при которой каждому значению одной переменной (аргументу) соответствует единственное значение другой переменной (функции), называется функциональной зависимостью или функцией.
### Определение. Множество всех значений, которые принимает аргумент, называется областью определения функции.
### Определение. Все значения, которые принимает функция, называются множеством значений функции.
### Определение. Значения аргумента, при которых значения функции равны нулю, называются нулями функции.
### Определение. Графиком функции называется множество всех точек координатной плоскости, абсциссы которых равны значениям аргумента, а ординаты — значениям функции.

## Формулы
$s = vt$
$m = 0,52 + 0,71 \cdot V$
$C = a + b \cdot s$
$y = 1,6x$

## Правила и алгоритмы
### Табличный способ задания функции
Функция задана таблично, если в первой строке таблицы записаны значения аргумента, а во второй — соответствующие значения функции.

### Графический способ задания функции
Функция задана графически, если ее график изображен на координатной плоскости. Значения аргумента отмечаются на оси абсцисс, а значения функции — на оси ординат.

### Аналитический способ задания функции
Функция задана аналитически, или формулой, если зависимость между переменными выражена формулой.

## Ключевые разобранные задания учебника
### 1. Какие из следующих зависимостей являются функциями: а) зависимость между числом купленных тетрадей и стоимостью покупки; б) зависимость между оценкой за контрольную работу и номером учащегося по списку в журнале; в) зависимость между длиной стороны квадрата и его площадью; г) зависимость между периметром треугольника и его наибольшей стороной?
Показывает, как определить, является ли зависимость функциональной, исходя из определения функции (каждому значению одной переменной соответствует единственное значение другой).

### 2. Величины смежных углов равны $\alpha$ и $\beta$. Задайте формулой зависимость $\beta$ от $\alpha$. Является ли зависимость функцией?
Показывает, как выразить функциональную зависимость формулой и проверить, является ли она функцией.

### 3. Функция задана таблично. Найдите: а) $f(4)$, $f(8,6)$, $f(10)$; б) $D(f)$; в) $E(f)$.
Показывает, как находить значения функции по таблице, а также определять область определения и множество значений функции, заданной таблично.

### 4. Функция задана формулой $f(x) = \frac{x}{x+5}$. Найдите $f(-4)$, $f(0)$, $f(3)$.
Показывает, как вычислять значения функции, заданной формулой, подставляя значения аргумента.

### 5. По графику функции $y = g(x)$, изображенному на рисунке 23, найдите $g(-1)$, $g(0)$, $g(2)$, $g(3)$.
Показывает, как находить значения функции по ее графику.

### 6. Функция $y=f(x)$ задана графиком (рис. 24). Найдите: а) нули функции; б) значения аргумента, при которых значения функции положительны; в) значения аргумента, при которых значения функции отрицательны.
Показывает, как определять нули функции, а также интервалы, где функция принимает положительные или отрицательные значения, по ее графику.

### 7. Являются ли кривые графиками функций (рис. 25)?
Показывает, как по графику определить, является ли он графиком функции, используя правило, что каждому значению абсциссы должно соответствовать единственное значение ординаты.

## Упражнения параграфа
1. Площадь прямоугольника с измерениями 7 м и $x$ м равна $S$. Является ли зависимость площади прямоугольника $S$ от $x$ функцией?
2. Можно ли найти нули функции, не используя график функции?
3.272. Какие из следующих зависимостей являются функциями: а) зависимость между количеством человек в вагоне поезда и номером вагона; б) зависимость между натуральным числом и количеством его делителей; в) зависимость между порядковым номером месяца в году и числом дней в этом месяце?
3.273. Длина, ширина и высота бассейна равны соответственно 25 м, 10 м и $x$ м. Бассейн заполнен водой на $\frac{4}{5}$ его высоты. Задайте формулой зависимость объема воды $V$ от высоты бассейна.
3.274. В таблице записаны результаты измерений температуры в течение суток.
3.275. Функция задана таблично. Найдите: а) $D(f)$; б) $E(f)$; в) $f(-3)$, $f(1)$, $f(12)$.
3.276. Функция задана формулой $f(x) = 5x - 1$. Найдите $f(-3)$, $f(0)$, $f(1)$, $f(100)$.
3.277. Из данных функций выберите те, для которых выполняется равенство $f(2) = 7$.
3.278. Верно ли, что $g(3) + g(5) = g(6)$, если функция задана формулой $g(x) = x^2 + 1$?
3.279. На рисунке 26 изображен график зависимости времени восхода солнца от месяца года в Минске.
3.280. Функция $y = f(x)$ задана графиком (рис. 27). Найдите $f(-5)$, $f(-3)$, $f(2)$, $f(4)$.
3.281. На рисунке 28 изображены графики функций. Выберите функцию, у которой: а) область определения содержит только отрицательные числа; б) множество значений содержит только положительные числа; в) $D(f): -2 \le x \le 5$; г) $E(f): -3 \le y \le 2$.
3.282. Изобразите в тетради график функции, у которой: а) область определения содержит только положительные числа; б) множество значений содержит только отрицательные числа; в) $D(f): -4 \le x \le 5$, а $E(f): -3 \le y \le 4$.
3.283. На рисунке 29 изображен график зависимости температуры $T$ воздуха от времени суток $t$.
3.284. Функция $y = f(x)$ задана графиком (рис. 30). Найдите: а) нули функции; б) при каких значениях аргумента значения функции положительны; в) при каких значениях аргумента значения функции отрицательны.
3.285. Изобразите в тетради график функции, нулями которой являются числа: а) $-2$ и $6$; б) $-5$; $-1$ и $4$.
3.286. На рисунке 31 изображены графики функций. Выберите функции: а) не имеющие нулей; б) для которых верно равенство $f(2) = 1$; в) принимающие положительные значения при $x = -2$.
3.296. На рисунке 34 изображен график движения туриста из города А в город В.
3.297. Функция $y = f(x)$ задана графиком (рис. 35). Найдите $f(-5)$, $f(-3)$, $f(0)$, $f(4)$.
3.298. Функция $y = f(x)$ задана графиком (рис. 36). Найдите: а) нули функции; б) при каких значениях аргумента значения функции положительны; в) при каких значениях аргумента значения функции отрицательны.
3.299. Найдите нули функции, заданной формулой: а) $f(x) = -3x + 2$; б) $f(x) = 9 - 4x$.
3.300. Какая из линий (рис. 37) является изображением графика функции? Объясните свой ответ.

## Замечания учебника
В приведенных примерах каждому значению одной величины (переменной) соответствует единственное значение другой величины.
В общем виде запись $T = f(t)$ означает, что $T$ — есть функция от $t$. Читается: «“тэ большое” равно “эф” от “тэ маленькое”». Вместо $f$ можно использовать любую другую букву латинского алфавита.
Область определения функции обозначается $D(f)$ (читается — «дэ» от «эф»).
Множество значений функции обозначается $E(f)$ (читается — «е» от «эф»).
Если нет описания процесса, который задает эта функция, то область определения функции — это те значения аргумента, при которых выражение, задающее функцию, имеет смысл.
Все значения аргумента из области определения функции поместить в таблицу невозможно. Однако вид графика можно представить и уточнить, увеличивая количество точек.
График функции дает информацию о ее свойствах: нулях функции, положительных и отрицательных значениях, области определения и множестве значений.
